<h1><%= @project.name %></h1>
<ul>
  <% @project.todos.each do |todo| %>
    <li>
      <%= link_to todo.name, todo_path(todo) %>
      <% if todo.completed %><span>✅</span><% end %>
    </li>
  <% end %>
</ul>

<%= button_to "Delete", project_path(@project), method: :delete, data: { turbo_confirm: "Tem certeza que deseja excluir esta trilha?" } %>
